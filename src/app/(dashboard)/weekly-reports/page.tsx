import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function WeeklyReportsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Weekly Reports</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-slate-500">Draft, submit, review, revision, dan approval flow dimulai dari modul ini.</p>
      </CardContent>
    </Card>
  );
}
